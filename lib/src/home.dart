import 'package:flutter/material.dart';
import 'package:artroad/src/ui/list.dart';

class Home extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: ListWidget(),
    );
  }
}